# Content

This directory contains MDX files for each project (separation of data from UI):

- Each project should have its own `.mdx` file
- MDX files combine Markdown with React components
- Keeps content separate from presentation logic

Example structure:
```
/src/content
  ├── project-1.mdx
  ├── project-2.mdx
  └── project-3.mdx
```
