# Components

This directory contains reusable React components for the portfolio:

- **Buttons**: Custom button components with different styles and behaviors
- **Cards**: Card components for displaying project information
- **Layout**: Layout components (Header, Footer, Navigation, etc.)

All components should be modular and reusable across different pages.
